RewriteEngine On

# Remove .php from URL
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.php -f
RewriteRule ^([^/]+)$ $1.php [L]

# Optional: Prevent direct access to .php URLs (Force clean URLs)
RewriteCond %{THE_REQUEST} \s/+(.+)\.php[\s?]
RewriteRule ^ %1 [R=301,L]
